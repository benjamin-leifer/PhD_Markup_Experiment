\documentclass[tikz,border=6pt]{standalone}
\usepackage[american]{circuitikz}

\begin{document}
\begin{circuitikz}[font=\Huge, scale=1.2, transform shape]

% --------- Main series line: L0 - R0 - (parallel1) - (parallel2) ----------
\draw
  (0,0)
  to[L, l=$L_0$] (2.2,0)
  to[R, l=$R_{sol}$] (4.4,0)

  % --------- Parallel block 1: R1 || CPE1 ----------
  (4.4,0) -- (5.2,0) coordinate (p1_in)
  (p1_in) to[short] (5.2, 1.5) to[R, l=$R_{SEI}$] (7.6, 1.5) -- (7.6,0)
  (p1_in) to[short] (5.2,-1.5) to[C, l=$\mathrm{CPE}_1$] (7.6,-1.5) -- (7.6,0)
  (7.6,0) -- (8.4,0)

  % --------- Parallel block 2: (R2 - W1) || CPE2 ----------
  (8.4,0) -- (9.2,0) coordinate (p2_in)
  % top branch: R2 in series with Warburg block
  (p2_in) to[short] (9.2, 1.5)
    to[R, l=$R_{CT}$] (11.6, 1.5)
    to[generic, l=$W_1$] (13.8, 1.5) -- (13.8,0)
  % bottom branch: CPE2
  (p2_in) to[short] (9.2,-1.5)
    to[C, l=$\mathrm{CPE}_2$] (13.8,-1.5) -- (13.8,0)

  % output node
  (13.8,0) -- (15.0,0);

% Optional: input/output dots
\draw (0,0) node[circ]{} (15.0,0) node[circ]{};

\end{circuitikz}
\end{document}
